#storefront-bff-ci.yaml

name: storefront-bff service ci

on:
  push:
    branches-ignore:
      - "rc_v*"
    tags-ignore:
      - "rc_v*"
    paths:
      - "storefront-bff/**"
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/storefront-bff-ci.yaml"
      - "pom.xml"
  pull_request:
    branches: ["main"]
    paths:
      - "storefront-bff/**"
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/storefront-bff-ci.yaml"
      - "pom.xml"
  workflow_dispatch:

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/actions
      - name: Run Maven Verify
        run: mvn clean verify -f storefront-bff
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set Docker Tag
        run: |
          echo "DOCKER_SHA=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./storefront-bff
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/yas-storefront-bff:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/yas-storefront-bff:${{ env.DOCKER_SHA }}

      - name: Generate GitHub App Token
        id: generate_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}

      - name: Update GitOps Repo
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git clone https://x-access-token:${{ steps.generate_token.outputs.token }}@github.com/${{ github.repository_owner }}/yas-gitops.git
          cd yas-gitops
          yq -i '.services."storefront-bff".tag = "${{ env.DOCKER_SHA }}"' values-dev.yaml
          if git diff --quiet; then
            echo "No changes to commit"
          else
            git add values-dev.yaml
            git commit -m "deploy: update storefront-bff image to ${{ env.DOCKER_SHA }} [skip ci]"
            git push origin main
          fi
