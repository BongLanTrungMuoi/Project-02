#search-ci.yaml
name: search service ci

on:
  push:
    branches-ignore:
      - "rc_v*"
    tags-ignore:
      - "rc_v*"
    paths:
      - "search/**"
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/search-ci.yaml"
      - "pom.xml"
  pull_request:
    branches: ["main"]
    paths:
      - "search/**"
      - ".github/workflows/actions/action.yaml"
      - ".github/workflows/search-ci.yaml"
      - "pom.xml"
  workflow_dispatch:

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/workflows/actions
      - name: Run Maven Build Command
        run: mvn clean install -pl search -am
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set Docker Tag
        run: |
          echo "DOCKER_SHA=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_ENV

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./search
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/yas-search:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/yas-search:${{ env.DOCKER_SHA }}

      - name: Generate GitHub App Token
        id: generate_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}

      - name: Update GitOps Repo
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git clone https://x-access-token:${{ steps.generate_token.outputs.token }}@github.com/${{ github.repository_owner }}/yas-gitops.git
          cd yas-gitops
          yq -i '.services.search.tag = "${{ env.DOCKER_SHA }}"' value-dev.yaml
          if git diff --quiet; then
            echo "No changes to commit"
          else
            git add value-dev.yaml
            git commit -m "deploy: update search image to ${{ env.DOCKER_SHA }} [skip ci]"
            git push origin main
          fi
